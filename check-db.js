import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function check() { const {data: p} = await supabase.from('products').select('*'); console.log('Products:', p?.length); const {data: s} = await supabase.from('storefront_settings').select('*'); console.log('Storefront:', s?.length); } check();  
